apiVersion: v1
kind: Service
metadata:
  name: {{ include "collaboration-relay.fullname" . }}
  labels:
    {{- include "collaboration-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "collaboration-relay.selectorLabels" . | nindent 4 }}
