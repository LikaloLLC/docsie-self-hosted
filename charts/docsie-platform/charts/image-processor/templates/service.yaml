apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-processor.fullname" . }}
  labels:
    {{- include "image-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "image-processor.selectorLabels" . | nindent 4 }}
