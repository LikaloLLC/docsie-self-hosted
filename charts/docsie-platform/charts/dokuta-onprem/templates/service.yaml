apiVersion: v1
kind: Service
metadata:
  name: {{ include "dokuta.fullname" . }}
  labels:
    {{- include "dokuta.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dokuta.selectorLabels" . | nindent 4 }}
