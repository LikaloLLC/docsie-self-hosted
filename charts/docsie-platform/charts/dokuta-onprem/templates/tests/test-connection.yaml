apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dokuta.fullname" . }}-test-connection"
  labels:
    {{- include "dokuta.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dokuta.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
