{{- if .Values.celeryBackground.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: docsie-celery-background
  labels:
    {{- include "docsie.labels" . | nindent 4 }}
    app.kubernetes.io/component: celery-background
spec:
  replicas: {{ .Values.celeryBackground.replicaCount }}
  {{- with .Values.celeryBackground.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "docsie.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: celery-background
  template:
    metadata:
      labels:
        {{- include "docsie.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: celery-background
    spec:
      enableServiceLinks: false
      serviceAccountName: {{ include "docsie.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "docsie.startupDependencies" . | nindent 6 }}
      containers:
        - name: docsie-celery-background
          image: {{ include "docsie.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.celeryBackground.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with include "docsie.workloadEnv" (dict "root" $ "component" .Values.celeryBackground) }}
          {{- . | nindent 10 }}
          {{- end }}
          envFrom:
            - secretRef:
                name: {{ include "docsie.secretName" . }}
            {{- if include "docsie.generatedSecretEnabled" . }}
            - secretRef:
                name: {{ include "docsie.generatedSecretName" . }}
            {{- end }}
            {{- with .Values.workload.extraEnvFrom }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- include "docsie.configVolumeMounts" . | nindent 10 }}
          resources:
            {{- toYaml .Values.celeryBackground.resources | nindent 12 }}
      {{- include "docsie.configVolumes" . | nindent 6 }}
      {{- with .Values.celeryBackground.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.celeryBackground.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.celeryBackground.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
