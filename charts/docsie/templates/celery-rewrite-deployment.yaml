{{- if .Values.celeryRewrite.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: docsie-celery-rewrite
  labels:
    {{- include "docsie.labels" . | nindent 4 }}
    app.kubernetes.io/component: celery-rewrite
spec:
  replicas: {{ .Values.celeryRewrite.replicaCount }}
  {{- with .Values.celeryRewrite.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "docsie.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: celery-rewrite
  template:
    metadata:
      labels:
        {{- include "docsie.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: celery-rewrite
    spec:
      enableServiceLinks: false
      serviceAccountName: {{ include "docsie.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "docsie.startupDependencies" . | nindent 6 }}
      containers:
        - name: docsie-celery-rewrite
          image: {{ include "docsie.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.celeryRewrite.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with include "docsie.workloadEnv" (dict "root" $ "component" .Values.celeryRewrite) }}
          {{- . | nindent 10 }}
          {{- end }}
          envFrom:
            - secretRef:
                name: {{ include "docsie.secretName" . }}
            {{- if include "docsie.generatedSecretEnabled" . }}
            - secretRef:
                name: {{ include "docsie.generatedSecretName" . }}
            {{- end }}
            {{- with .Values.workload.extraEnvFrom }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- include "docsie.configVolumeMounts" . | nindent 10 }}
          resources:
            {{- toYaml .Values.celeryRewrite.resources | nindent 12 }}
      {{- include "docsie.configVolumes" . | nindent 6 }}
      {{- with .Values.celeryRewrite.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.celeryRewrite.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.celeryRewrite.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
